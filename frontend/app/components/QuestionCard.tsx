


type QuestionCardProps = {
    userName: string;
    postingTime: Date;
    likeCount: number;
    bookmarkCount: number;
    replyCount: number;
    tagNames: string[];

};
export default function QuestionCard({ userName, postingTime, likeCount, bookmarkCount, replyCount, tagNames }: QuestionCardProps) {

    return (
        <div >
        </div>
    );
}